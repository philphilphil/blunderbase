{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The desktop window and its bundled backend",
  "windows": ["main"],
  "permissions": ["core:default"]
}
